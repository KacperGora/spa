import { deleteDoc, deleteField, doc, updateDoc } from "firebase/firestore";

import { db } from "../firebase";

async function cancelMeeting(key, loggedUserMail, usersMeetings) {
 
  
}

export default cancelMeeting;
